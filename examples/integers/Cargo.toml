[package]
name = "integers"
version = "0.1.0"
authors = ["Jake Goulding <jake.goulding@gmail.com>"]

# [excerpt]
[dependencies]
libc = "*"

[lib]
crate-type = ["cdylib"]
# [excerpt]
name = "integers"
